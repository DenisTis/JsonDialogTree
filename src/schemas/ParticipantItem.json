{
    "$id": "ParticipantItem.json",
    "description": "Participant replic in the dialog",
    "type": "object",
    "properties": {
        "participantName": {
            "type": "string"
        },
        "textId": {
            "type": "string"
        }
    },
    "required": ["participantName", "textId"]
}